// The `NoResults` function returns a paragraph element with the text "No Results were found. Please try again!"
function NoResults() {
  return <p>No Results were found. Please try again!</p>;
}

// Export the `NoResults` function as the default export for the module
export default NoResults;
