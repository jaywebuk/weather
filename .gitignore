# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# This section ignores dependencies to reduce clutter and avoid committing large dependency directories.
/node_modules
/.pnp
.pnp.js

# This section ignores testing-related files and directories.
/coverage

# This section ignores production-related files and directories.
/build

# These are common files and directories that are excluded for various reasons, such as being system-specific or containing sensitive information.
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

# These are log files generated by npm and yarn that can be safely ignored.
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# These are specific files that have been excluded for various reasons.
Search.js
.env
city.json
weather*.json*
test.txt
test.json
test.js
README.md.old
useToggleSection.js
.env.stage
*.pem